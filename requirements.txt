# LM Studio Multi-Model Orchestrator 
# Python 3.8+

# External dependencies (pip install -r requirements.txt)
requests>=2.28.0    # all LM Studio API calls
psutil>=5.9.0       # RAM readout in the resource monitor (new in v29)

# Built-in modules (no installation needed):
# - tkinter (Python GUI - may need a separate install on Linux, e.g. python3-tk)
# - os, sys, subprocess, shutil, glob (files, paths, restart, backups)
# - threading, time (background workers and pacing)
# - datetime (timestamps for logs, transcripts and backups)
# - json (settings, transcripts, constraint configs, master prompt file)
# - re, string, difflib (constraints, prompt validation, duplicate detection)
# - abc, typing (Constraint Engine base classes and type hints)

# Optional (not a Python package):
# - nvidia-smi on the PATH for the VRAM readout (NVIDIA GPUs only)
